# Codex hook companion

For the user guide, start with [`README-CODEX.md`](../README-CODEX.md).

The implementation reference is [`docs/CODEX-HOOKS.md`](../docs/CODEX-HOOKS.md). The two files next to this note are the runtime configuration and command script:

- [`hooks.json`](hooks.json) — lifecycle hook registration;
- [`hooks/tasks_lifecycle.py`](hooks/tasks_lifecycle.py) — hook behavior and explicit command equivalents.
